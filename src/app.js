/**
 * Created by edward.yuen on 10/13/2016.
 */
var createCreating = function (message, name) {
    return meesage + name;
}

var arrowGreeting = (message, name) => message + name;
console.log(arrowGreeting("hello ", "edward"));